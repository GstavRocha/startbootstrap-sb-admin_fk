{
  "name": "apirequests",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "npx tsc --watch",
    "start": "find . -path './node_modules' -prune -o -wholename '*/**/*.js' -exec node {} +"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "axios": "^1.6.7"
  },
  "dependencies": {
    "dotenv": "^16.4.4"
  }
}
